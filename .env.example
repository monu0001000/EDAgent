# Get a free key (no credit card required) at https://console.groq.com/keys
GROQ_API_KEY=your-free-groq-api-key-here

# Optional: override the default model. Current default is openai/gpt-oss-120b
# (migrated from llama-3.3-70b-versatile after Groq deprecated it on Aug 16,
# 2026 — see README's "Model migration" note). Check
# https://console.groq.com/docs/models for what's currently available if this
# one ever gets deprecated too.
# GROQ_MODEL=openai/gpt-oss-120b
